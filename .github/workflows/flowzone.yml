name: Flowzone

on:
  pull_request:
    types: [opened, synchronize, closed]
    branches:
      - "main"
      - "master"

jobs:
  flowzone:
    name: Flowzone
    uses: product-os/flowzone/.github/workflows/flowzone.yml@master
    secrets: inherit
    with:
      balena_slugs: 'balenalabs/multiroom-rpi,balenalabs/multiroom-armv7hf,balenalabs/multiroom-aarch64,balenalabs/multiroom-amd64'
